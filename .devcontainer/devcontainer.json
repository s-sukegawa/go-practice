{
  "name": "Go Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [8080],
  "postCreateCommand": "go mod download && go install github.com/air-verse/air@v1.61.7 && curl -fsSL https://claude.ai/install.sh | bash && git config core.hooksPath .githooks",
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ],
      "settings": {
        "go.toolsManagement.autoUpdate": true
      }
    }
  }
}
